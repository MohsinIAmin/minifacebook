module.exports = {
    db: {
        host: "localhost",
        user: "minifacebook",
        password: "PuxBnWhh89HxDbZN",
        database: "minifacebook"
    },
    listPerPage: 10,
};

// CREATE TABLE`minifacebook`.`users`(`id` INT(11) NOT NULL AUTO_INCREMENT, `username` VARCHAR(255) NOT NULL, `password` VARCHAR(255) NOT NULL, PRIMARY KEY(`id`)) ENGINE = InnoDB;
